# Scratch and generated files — keeps `git status` clean during development.

# Agent/editor working directories
.freebuff/

# Temporary outputs and captures
tmp_*
SHELL_tree
jnk*
j_Copilot
j_original
out_*.txt

# Archived old tool versions (kept on disk, out of version control)
_Old_Stuff/

# Compiled artifacts (sources prefix_children*.c are tracked)
prefix_children

# Editor backups
*.bak
